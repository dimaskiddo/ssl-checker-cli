# Port to check SSL certificates on (Default: 443)
SSL_CHECK_PORT=443

# Timeout limit in milliseconds before dropping a connection (Default: 5000)
SSL_CHECK_TIMEOUT=5000

# Days threshold remaining to trigger a "⚠️ UPDATE SOON" warning status (Default: 14)
SSL_CHECK_WARN_DAYS=14
